<!DOCTYPE html>
<html>
<head>
  <title>IawGitApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
      <% if !@gitSession.nil? %>
          <div class="navbar-header">
            <a class="navbar-brand" href="#">Project name</a>
          </div>
          <ul>
            <p class="navbar-text pull-right navbar-right"><%= link_to "Sign out", :controller => :git_hub, :action => :sign_out%> </p>
            <p class="navbar-text pull-right navbar-right">Signed in as <span class="text-info"> <%= @currentUser%></span></p>
            <p class="navbar-text pull-right navbar-right">Module</p>
          </ul>
    <%else%>
          <div class="brand">
          <div class="navbar-header">
            <a class="navbar-brand" href="#">Project name</a>
          </div>
        </div>
      <%end%>
  </div>

</nav>
  <div class="container content">
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <%= yield %>
</div>
</body>
</html>
